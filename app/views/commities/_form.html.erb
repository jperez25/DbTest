<%= form_with(model: commity, local: true) do |form| %>
  <% if commity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commity.errors.count, "error") %> prohibited this commity from being saved:</h2>

      <ul>
      <% commity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :commity_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
